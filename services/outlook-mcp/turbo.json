{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**/*"]
    },
    "db:generate": {
      "dependsOn": ["db:generate"],
      "outputs": ["drizzle/**/*"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "dev": {
      "inputs": ["$TURBO_DEFAULT$", "src/**/*"],
      "dependsOn": ["build"],
      "cache": false,
      "persistent": true,
      "interruptible": true
    }
  }
}
