import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'dollar-sign',
  systemPrompt: 'Use to look up payment fees. Returns the full fee schedule; no filters available.',
});
