import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'search',
  systemPrompt: 'Use to look up transaction stop investigations. Returns all open investigations; no filters available.',
});
