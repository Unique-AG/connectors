import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'percent',
  systemPrompt:
    'Use to look up interest conditions applied to products. Returns the full list; no filters available.',
});
