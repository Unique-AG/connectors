import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'book-open',
  systemPrompt: 'Use to look up balance type codes and their definitions. Returns the full list.',
});
