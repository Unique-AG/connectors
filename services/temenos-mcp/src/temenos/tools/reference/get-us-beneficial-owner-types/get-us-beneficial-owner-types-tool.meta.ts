import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'flag',
  systemPrompt: 'Use to look up US beneficial owner type codes. Returns the full list.',
});
