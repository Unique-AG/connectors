import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'percent',
  systemPrompt:
    'Use to look up interest rate description texts. Filter by recordId or displayName.',
});
