import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'activity',
  systemPrompt: 'Use to look up treasury dealer desks. Filter by recordId or displayName.',
});
