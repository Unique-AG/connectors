import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'book-open',
  systemPrompt:
    'Use to look up cheque types available for a product. Filter by productName to get cheque types for a specific product.',
});
