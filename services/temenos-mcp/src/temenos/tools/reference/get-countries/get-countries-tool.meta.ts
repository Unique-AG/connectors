import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'globe',
  systemPrompt: 'Use to look up country codes. Filter by recordId to retrieve a specific country.',
});
