import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'calendar',
  systemPrompt:
    'Use to look up system business dates including the current, next, and last working date for each company.',
});
