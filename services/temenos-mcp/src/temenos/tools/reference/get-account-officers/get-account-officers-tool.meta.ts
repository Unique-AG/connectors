import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'user',
  systemPrompt: 'Use to look up account officers. Returns the full list; no filters available.',
});
