import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'file-text',
  systemPrompt: 'Use to look up loan covenant definitions for the US model bank.',
});
