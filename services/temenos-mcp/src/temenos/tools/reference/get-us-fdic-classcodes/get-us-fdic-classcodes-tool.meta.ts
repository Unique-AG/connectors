import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'shield',
  systemPrompt: 'Use to look up FDIC subclassification codes for US regulatory reporting.',
});
