import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'table',
  systemPrompt: 'Use to look up values from Temenos EB.LOOKUP tables. Provide virtualTable to query a specific lookup table.',
});
