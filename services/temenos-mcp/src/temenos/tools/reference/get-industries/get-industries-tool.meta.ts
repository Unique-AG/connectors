import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'building',
  systemPrompt: 'Use to look up industry classification codes. Returns the full list.',
});
