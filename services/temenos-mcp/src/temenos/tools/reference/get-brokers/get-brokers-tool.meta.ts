import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'briefcase',
  systemPrompt: 'Use to look up brokers registered in the system. Returns the full list.',
});
