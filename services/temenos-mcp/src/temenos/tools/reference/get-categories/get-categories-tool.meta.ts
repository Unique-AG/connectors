import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'tag',
  systemPrompt:
    'Use to look up internal product category codes. Filter by recordId for a specific category.',
});
