import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'trending-up',
  systemPrompt:
    'Use to look up option expiration transactions. Filter by portfolioId or instrumentId.',
});
