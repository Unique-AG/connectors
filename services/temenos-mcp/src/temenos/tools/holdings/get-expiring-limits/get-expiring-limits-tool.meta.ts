import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'clock',
  systemPrompt: 'Use to find credit limits that are expiring. Filter by expiryDate to see limits expiring on a specific date.',
});
