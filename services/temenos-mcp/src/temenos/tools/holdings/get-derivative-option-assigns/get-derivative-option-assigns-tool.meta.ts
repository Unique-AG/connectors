import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'trending-up',
  systemPrompt:
    'Use to look up option assignment transactions. Filter by portfolioId, instrumentId, or tradeStatus to narrow results.',
});
