import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'landmark',
  systemPrompt: 'Use to look up Vostro accounts. Filter by currencyId to find accounts in a specific currency.',
});
