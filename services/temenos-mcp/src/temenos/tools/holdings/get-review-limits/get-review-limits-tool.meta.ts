import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'clock',
  systemPrompt:
    'Use to find credit limits scheduled for review. Filter by limitReviewDate to see limits due for review on a specific date.',
});
