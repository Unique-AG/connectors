import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'landmark',
  systemPrompt: 'Use to retrieve all Nostro accounts. No filters available; returns the full list.',
});
