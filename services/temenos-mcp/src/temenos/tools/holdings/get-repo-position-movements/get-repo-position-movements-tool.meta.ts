import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'repeat',
  systemPrompt:
    'Use to track position movements in repurchase agreements. Filter by portfolioId or instrumentId.',
});
