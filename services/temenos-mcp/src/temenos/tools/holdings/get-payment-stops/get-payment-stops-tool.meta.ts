import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'x-circle',
  systemPrompt: 'Use to find payment stops on accounts. Filter by customerId to find all stops for a specific customer.',
});
