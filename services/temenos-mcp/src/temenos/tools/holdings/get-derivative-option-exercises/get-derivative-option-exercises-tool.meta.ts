import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'trending-up',
  systemPrompt: 'Use to look up option exercise transactions. Filter by portfolioId, instrumentId, or tradeStatus.',
});
