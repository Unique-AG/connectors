import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'repeat',
  systemPrompt: 'Use to track position movements in reverse repurchase agreements.',
});
