import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'settings',
  systemPrompt: 'Use to look up preferences for external users. Filter by recordId to retrieve a specific user\'s preferences.',
});
