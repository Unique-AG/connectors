import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'users',
  systemPrompt:
    'Use to look up system participants. Filter by accountOfficer to find participants managed by a specific officer.',
});
