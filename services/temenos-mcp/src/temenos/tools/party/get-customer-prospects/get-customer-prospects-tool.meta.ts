import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'user-plus',
  systemPrompt: 'Use to retrieve all customer prospects. Returns the full list; no filters available.',
});
