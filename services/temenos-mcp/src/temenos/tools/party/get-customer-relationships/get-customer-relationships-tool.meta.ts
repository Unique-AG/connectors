import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'users',
  systemPrompt: 'Use to look up relationships between customers. Filter by partyId or relationPartyId to explore a specific customer\'s connections.',
});
