import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'message-square',
  systemPrompt: 'Use to look up secure messages. Filter by toCustomerId or fromCustomerId to find messages for a specific customer.',
});
