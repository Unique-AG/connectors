nameOverride: temenos-mcp
fullnameOverride: temenos-mcp
selectorComponentLabel: server

podLabels:
  logging.unique.app/format: pino-json

env:
  LOG_LEVEL: info
  MAX_HEAP_MB: 400
  NODE_ENV: production
  OTEL_EXPORTER_PROMETHEUS_HOST: "0.0.0.0"
  OTEL_EXPORTER_PROMETHEUS_PORT: "51346"
  OTEL_METRICS_EXPORTER: "prometheus"
  OTEL_EXPORTER_OTLP_ENDPOINT:
    if: '{{ .Values.internalServices.dependencies.otelTraces.enabled }}'
    value: '{{ include "base.internalService.url" (dict "root" . "dep" .Values.internalServices.dependencies.otelTraces) }}'
  OTEL_EXPORTER_OTLP_TRACES_ENDPOINT:
    if: '{{ .Values.internalServices.dependencies.otelTraces.enabled }}'
    value: '{{ include "base.internalService.url" (dict "root" . "dep" .Values.internalServices.dependencies.otelTraces) }}/v1/traces'

envVars: []

mcpConfig:
  app: {}
  temenos:
    apiBaseUrl: https://api.temenos.com/api/v1.0.0

image:
  registry: ghcr.io
  repository: unique-ag/connectors/services/temenos-mcp
  tag: 0.0.0 # x-release-please-version
  pullPolicy: IfNotPresent

ports:
  application: 9543
  metrics: 51346

resources:
  limits:
    memory: 512Mi
  requests:
    cpu: 25m
    memory: 256Mi

probes:
  enabled: true
  liveness:
    failureThreshold: 6
    httpGet:
      path: /probe
      port: http
    initialDelaySeconds: 10
    periodSeconds: 5
  readiness:
    failureThreshold: 6
    httpGet:
      path: /probe
      port: http
    initialDelaySeconds: 10
    periodSeconds: 5
  startup:
    failureThreshold: 30
    httpGet:
      path: /probe
      port: http
    initialDelaySeconds: 10
    periodSeconds: 10

serviceAccount:
  enabled: true

volumes:
  - name: tmp
    emptyDir:
      sizeLimit: 64Mi

volumeMounts:
  - name: tmp
    mountPath: /tmp

postgresql:
  enabled: false

rabbitmq:
  enabled: false

service:
  port: 80

internalServices:
  dependencies:
    otelTraces:
      enabled: false
      name: otel-traces
      servicePort: 4318
  dependents:
    ingressGateway:
      name: gateway
      namespace: system

routes:
  hostname: ""
  pathPrefix: /
  auth:
    jwt: false

hooks:
  migration:
    enabled: false

networkPolicy:
  enabled: false
  flavor: cilium
  enableDefaultDeny:
    ingress: true
    egress: true
  baseline:
    prometheus:
      namespace: system
    ephemeralPorts:
      enabled: false
    egress:
      temenos:
        toFQDNs:
          - matchName: api.temenos.com
          - matchPattern: "*.temenos.com"
        toPorts:
          - ports:
              - port: "443"
                protocol: TCP
