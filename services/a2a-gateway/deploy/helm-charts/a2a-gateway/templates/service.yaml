apiVersion: v1
kind: Service
metadata:
  name: {{ include "a2a-gateway.fullname" . }}
  labels:
    {{- include "a2a-gateway.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "a2a-gateway.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
