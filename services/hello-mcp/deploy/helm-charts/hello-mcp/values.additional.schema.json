{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Hello MCP Values",
  "description": "Values for the Hello MCP Helm chart. Minimal no-auth Python FastMCP server. Exposure uses base chart Gateway API routes (no custom Ingress).",
  "type": "object",
  "properties": {}
}
