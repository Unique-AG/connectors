# =================================================================
# APPLICATION CONFIGURATION
# =================================================================

NODE_ENV=development
LOG_LEVEL=debug
LOGS_DIAGNOSTICS_DATA_POLICY=conceal
PORT=9542

# =================================================================
# TENANT CONFIGURATION
# =================================================================

# Path pattern to tenant configuration YAML file
# All SharePoint, Unique API, and Processing settings are now in the YAML file
# Supports glob pattern like /app/tenant-configs/*-tenant-config.yaml
# Example files:
#   - default-tenant-config.example-config-file.yaml (static site list)
#   - default-tenant-config.example-sharepoint-list.yaml (dynamic from SharePoint)
TENANT_CONFIG_PATH_PATTERN=./default-tenant-config.yaml

# =================================================================
# SECRETS (only if needed based on auth mode)
# =================================================================

# For certificate auth mode (if private key is encrypted)
# SHAREPOINT_AUTH_PRIVATE_KEY_PASSWORD=your-key-password

# For external Unique API auth mode (when using Zitadel)
# ZITADEL_CLIENT_SECRET=your-zitadel-client-secret

# =================================================================
# HTTP PROXY CONFIGURATION (optional)
# =================================================================
#
# Proxy authentication mode: none | username_password | ssl_tls
# PROXY_AUTH_MODE=none
#
# Proxy connection settings (required when PROXY_AUTH_MODE is not 'none')
# PROXY_PROTOCOL=http
# PROXY_HOST=proxy.example.com
# PROXY_PORT=8080
#
# For username_password mode
# PROXY_USERNAME=your-proxy-username
# PROXY_PASSWORD=your-proxy-password
#
# For ssl_tls mode (mTLS client certificate)
# PROXY_SSL_CERT_PATH=/path/to/client-cert.pem
# PROXY_SSL_KEY_PATH=/path/to/client-key.pem
#
# Optional: Custom CA bundle for proxy TLS verification
# PROXY_SSL_CA_BUNDLE_PATH=/path/to/ca-bundle.pem
#
# Optional: Additional proxy headers as JSON string
# PROXY_HEADERS={"X-Custom-Header": "value"}
#
# =================================================================
# MONITORING CONFIGURATION
# =================================================================

OTEL_METRICS_EXPORTER=prometheus
OTEL_EXPORTER_PROMETHEUS_HOST=0.0.0.0
OTEL_EXPORTER_PROMETHEUS_PORT=51346