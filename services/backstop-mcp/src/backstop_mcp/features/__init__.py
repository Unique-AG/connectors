"""Feature implementations: what this connector actually does.

Empty for now — Backstop credential bridging (`auth`), CRM custom-field schema discovery
(`custom_fields`), name-to-record lookup (`party_resolver`), and the rest of the domain logic
land in later PRs, stacked on top of this scaffolding.
"""
