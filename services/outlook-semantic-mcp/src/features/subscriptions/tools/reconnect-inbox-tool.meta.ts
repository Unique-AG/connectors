import { createMeta } from '@unique-ag/mcp-server-module';

export const META = createMeta({
  icon: 'play',
  systemPrompt:
    'Re-establishes the inbox subscription for Outlook email ingestion. If already active, inform the user that ingestion is already running.',
});
