name: "[confluence-connector] Warm Docker Build Cache (nightly)"

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch: {}

jobs:
  warm:
    uses: ./.github/workflows/_template-cache.yaml
    with:
      service-name: confluence-connector
      service-path: services/confluence-connector
      dockerfile: services/confluence-connector/deploy/Dockerfile
