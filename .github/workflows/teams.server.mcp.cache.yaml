name: "[mcp/server/teams] Warm Docker Build Cache (nightly)"

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch: {}

jobs:
  warm:
    uses: ./.github/workflows/_template-cache.yaml
    with:
      service-name: teams-mcp
      service-path: services/teams-mcp
      dockerfile: services/teams-mcp/deploy/Dockerfile
